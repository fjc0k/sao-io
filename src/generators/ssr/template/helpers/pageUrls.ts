export const pageUrls = {
  // @index('../pages/**/*.{tsx,jsx}', (pp, cc, ee, xx = pp.path.replace('../pages', '')) => `${cc.pascalCase(x)}: '${(xx = xx.replace(/\/index$/g, '')) === '' ? '/' : xx}',`)
  HomeIndex: '/home',
  Index: '/',
  // @endindex
}
